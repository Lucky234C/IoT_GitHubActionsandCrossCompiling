name: Compile C/C++ Program

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Compile C/C++ Program
      run: |
        g++ main.cpp -o my_program
